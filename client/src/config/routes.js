const baseUrl = '/library-app';

export const routes = {
    HOMEPAGE: `${baseUrl}/`,
    GALLERY: `${baseUrl}/gallery`,
    BOOK_DETAILS: `${baseUrl}/view`,
    EDIT_BOOK: `${baseUrl}/edit`,
    CREATE_BOOK: `${baseUrl}/create`,
};
