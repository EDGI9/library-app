export default function Loading(params) {
    return (
        <div
            data-testid="qa-loading"
            className="flex flex-column justify-center p-3 bg-white/30"
        >
            <p className="text-sm font-bold">Loading...</p>
        </div>
    );
}
